<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if controller.class.to_s.match(/^BigAuth:/) %>
      <%= stylesheet_link_tag "big_auth" %>
    <% end %>
    <title><%= content_for?(:title) ? "#{yield(:title)} :: Majority Report" : "Majority Report" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", media: "all" %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', rel: 'shortcut icon' %>

    <%# javascript_include_tag "application" %>
    <%= yield :head %>
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Majority Report</a>

        <div class="navbar-collapse collapse navbar-responsive-collapse navbar-right">
          <ul class="nav navbar-nav">
            <% if current_user %>
              <li>
                <a href="/messages" class="<%= 'orange' if current_user.orange %>">
                  <i class="fa fa-envelope-o"></i>&nbsp;
                  Hello, <%= current_user.username %>
                </a>
              </li>
              <li><%= link_to 'My Profile', build_profile_path(current_user) %></li>
              <li><%= link_to 'Sign Out', "/logout", :method => :delete %></li>
            <% else %>
              <li><%= link_to 'Login / Sign Up', "/login" %></li>
            <% end %>
          </ul>
        </div>

        <form action="/search" method="GET" class="navbar-collapse collapse navbar-responsive-collapse navbar-form navbar-right" role="search">
          <div class="form-group col-xs-9 col-sm-9 col-lg-8">
            <input type="text" name="query" class="form-control" placeholder="Search">
          </div>
          <div class="form-group col-xs-2 col-sm-2">
            <button type="submit" class="btn btn-default">Go</button>
          </div>
        </form>

      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>

        <%= yield :sidebar %>

      </div><!--/row-->

      <br/>

      <%= render :partial => 'shared/bitcoin' %>

      <footer>
        <p>&copy; Majority.Report <%= Time.now.year %> |
          <%= link_to 'source code', 'https://github.com/phezco/phez' %> |
          <%= link_to 'privacy policy', privacy_home_index_path %> |
          <%= link_to 'api', apidocs_home_index_path %> |
          <%= link_to 'markdown help', 'http://daringfireball.net/projects/markdown/syntax', target: '_blank' %>
          <% if Figaro.env.enable_screenshots %>
            | <%= raw(Figaro.env.pagekeeper_link_code) %>
          <% end %>
        </p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
